{
  "name": "teachmate-sms-gateway",
  "version": "1.0.0",
  "description": "TeachMate SMS backend — keeps Africa's Talking API key server-side and proxies SMS sends from the TeachMate frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "africastalking": "^0.7.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
